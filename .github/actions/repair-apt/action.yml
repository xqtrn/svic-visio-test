name: repair-apt
description: Survive Ubuntu Azure / Google Chrome apt hash mismatches on GitHub-hosted runners
runs:
  using: composite
  steps:
    - name: Repair apt mirrors
      shell: bash
      run: bash "${{ github.action_path }}/repair-apt.sh"
